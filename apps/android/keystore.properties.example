# 릴리즈 서명 정보 — 이 파일을 keystore.properties 로 복사한 뒤 실제 값을 채운다.
# keystore.properties 는 .gitignore 대상이므로 저장소에 올라가지 않는다.
# 파일이 없으면 release 서명이 생략되고 unsigned 로 빌드된다(디버그 빌드는 정상 동작).

storeFile=/absolute/path/to/freetiful.keystore
storePassword=
keyAlias=
keyPassword=
storeType=pkcs12
